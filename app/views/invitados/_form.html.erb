<%= form_for(@invitado) do |f| %>
  <% if @invitado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invitado.errors.count, "error") %> prohibited this invitado from being saved:</h2>

      <ul>
      <% @invitado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_consultora %><br>
    <%= f.text_field :id_consultora %>
  </div>
  <div class="field">
    <%= f.label :emailInvitado %><br>
    <%= f.text_field :emailInvitado %>
  </div>
  <div class="field">
    <%= f.label :emailConsultor %><br>
    <%= f.text_field :emailConsultor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
